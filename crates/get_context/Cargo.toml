[package]
name = "get_context"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lockfile_utils = { path = "../lockfile_utils" }
futures = "0.3.19"